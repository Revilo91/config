# Stub secrets file used only for CI config checks (see .github/workflows/main.yml).
# It is copied to secrets.yaml so `home-assistant --script check_config` has
# something to resolve !secret references against. Do not put real secrets here.
